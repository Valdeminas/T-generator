@model IEnumerable<T_generator.Models.AmazonBrowseNode>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BrowseNode)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BrowseNode)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AmazonBrowseNodeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AmazonBrowseNodeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AmazonBrowseNodeID })
        </td>
    </tr>
}

</table>
