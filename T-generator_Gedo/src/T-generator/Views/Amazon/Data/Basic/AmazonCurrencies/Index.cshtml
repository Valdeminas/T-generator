@model IEnumerable<T_generator.Models.Amazon.Data.Basic.AmazonCurrency>

@{
    ViewData["Title"] = "Index";
}

<h2>Currency</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Currency)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Currency)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AmazonCurrencyID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AmazonCurrencyID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AmazonCurrencyID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
